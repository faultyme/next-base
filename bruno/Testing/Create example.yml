info:
  name: Create example
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/examples"
  body:
    type: json
    data: |-
      {
        "name": "kunal"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
